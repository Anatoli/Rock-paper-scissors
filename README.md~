# Rock-paper-scissors
Rock-paper-scissors

As this is the maven application, run the folloing command:

mvn clean compile exec:java -DskipTests=true

Once the app is up and running, you can access the UI through:

http://localhost:8080/index.html

